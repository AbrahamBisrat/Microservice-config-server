spring:
  jackson:
    serialization:
      write-dates-as-timestamps: false
  cloud:
    vault:
      token: hvs.AcnzRzHYehiXhMnRQsTQW3tS
  rabbitmq:
    host: localhost
    port: 5672
logging:
  file:
    name: /Coding/Logging/${spring.application.name}/log

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

test:
  name: "meawuuu"